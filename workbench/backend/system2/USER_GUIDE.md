# Document Processing Component

This component owns source-preserving parsing, canonical document structure and versioned material content inside Workbench. Use the root [User Guide](../../../USER_GUIDE.md) and [Environment Guide](../../../ENVIRONMENT.md). The storage contract is [here](../../contracts/storage-and-exchange.md). `pyproject.toml` and `uv.lock` own Python dependencies; optional CLI exports do not replace the human review workflow.
