# Isolated browser workflow evidence

Engineering observations only, using copied public PA001 and synthetic materials. Reviewer names are test identities from the approved roster. All acceptance clicks below are simulated state-transition tests, not a review by the named colleague, legal validation, or evidence of automatic extraction quality. The normal business workspace was not used for these writes.

## Current evidence

| Scenario | Expected | Observed in actual browser | Remaining acceptance |
| --- | --- | --- | --- |
| 1. Find / read / explicitly extract | Opening only reads; Extract creates a candidate | PA001 snapshot PA001-001 opened; explicit Extract generated 412 blocks. TS002 XLSX and TS003 PDF were also explicitly extracted; post-/03 startup and TS002 re-extraction observed, no Requirement processing | Recheck relevant paths on the next frozen candidate |
| 2. Status / original correspondence | True processed and unresolved scope; selected content locates its source | PA001 exact paragraph selected; TS003 shows processed 3 / usable 1 / unresolved 2 and page-2 original preview; TS002 preserves sheet/range/formula associations | Running-task reload/resumed polling now observed on TS004 ([evidence](source-failure/RESULTS.md)); full table navigation and frozen-candidate recheck remain |
| 3. Edit / save / reopen | Text, numbering, hierarchy, order and table edits survive reopen | PA001 manual text retained; TS002 TEST 1.1, parent change, B3=14, inserted row, merge-height change and reordered table survived Save / Reload saved | Recheck on next frozen candidate; broader formats remain separate |
| 4. Refresh / close / service restart | Saved work resumes without losing input or version identity | PA001 reviewer revision 3 reopened; TS003 manual revision 5 survived service restart; TS002 saved work survived reload and offline-save retry; comparison order choice restored after post-v2 service restart and return/reopen | TS004 running-task refresh/reopen and later personal revision-12 post-application reopen passed ([evidence](source-failure/RESULTS.md)); integrated frozen recheck remains |
| 5. Re-extract after manual edits | Old work remains; changes require explicit comparison and adoption | TS002 manual differences retained; v2 separated order from fields. Candidate order applied while manual numbering, top-level relationship, 14, inserted row, formula and notes were kept; personal revision 14 reopened correctly | TS004 empty re-extraction retained manual text through an explicit Keep current/application ([evidence](source-failure/RESULTS.md)); other partial combinations and changed-ID decisions remain |
| 6. Draft / master / confirmation / Requirement states | Each status names its own version and operation | PA001 master adoption produced draft, separate confirmation archived revision 2; later return adoption produced revision 3 draft. Queue distinguishes personal/master. TS004 saved checks reopen at revision 5 without confirmation; subsequent text edit/reopen produces revision 6, content version 2 and 0/1 checked, with explicit missing-range navigation ([evidence](review-confirmation/RESULTS.md)). Process remains Not connected | Full Requirement candidate-review path is unconnected and unmeasured |
| 7. Archive / follow-up | Recheck preserves accepted history and prior confirmation | PA001 accepted revision 2 retained; task created/exported; assigned receiver saved/reopened progress and recorded/reopened a resource finding, with original confirmation and all material rows unchanged ([evidence](archive-inspection/RESULTS.md)) | Explicit pass/resolution branch on fully checked bounded scope and returned task result remain |
| 8. Work-package round trip | Import validates; comparison/adoption and receipts are separate | Coordinator work -> receiver edit/submission -> coordinator comparison/adoption -> receiver receipt import observed. Receipt re-import accurately reports no further adoption needed | Returned text conflict, saved/resumed choice, explicit master-4 adoption and receipt import passed ([evidence](return-conflict/RESULTS.md)); returned table-cell conflicts, Windows and real-colleague acceptance remain |
| 9. Repeat / multi-tab / stale versions | No duplicate application or silent overwrites | Two actual TS002 tabs rejected stale save, retained local14, displayed saved13, and required explicit merge then Save. Identical receipt re-import safe. Stable prepare identity passes isolated workload tests | Actual delayed double-click/request replay after identity fix |
| 10. Failure / partial / empty / missing original / locks | Truthful state, retained input and actionable recovery | TS003 partial PDF retains limitations after manual save; offline TS002 Save retains exact input and Retry succeeds. TS003 save under a real SQLite write lock retained its edited text; lock release/retry/reopen saved revision 6 with 0/3 checks. TS004 empty HTML reports no usable content; a separately attributed manual note survives save/reopen with 0/1 checks. Opening malformed TS005 PDF fails while retaining TS004 and restoring navigation. Post-/03 module-loading startup failure reproduced and corrected | Actual TS004 job failure and missing pinned original/retry now observed with unsaved input preserved ([evidence](source-failure/RESULTS.md)); integrated frozen-candidate regression remains |

An early click on Review my changes did not immediately produce an observable response. A later retry opened the comparison. This is not recorded as duplicate-request acceptance; dedicated delayed-response testing remains required.

## Environments

- Coordinator: `workbench/runtime/product-readiness-20260912/fixture/`, temporary URL recorded in its `server.json` (currently port 55542).
- Receiver: `workbench/runtime/product-readiness-20260912/reviewer/`, temporary URL recorded in its `server.json` (currently port 58814).
- Fault-only fixture: `workbench/runtime/product-readiness-20260912/ui-fault-fixture/`, isolated port 60905, seven locally copied/authored sources. The empty and deliberately malformed originals are engineering specimens; no normal source or review decision was changed.
- Both run the actual local Application/Handler with independent stores and coordinator business schedules disabled. The owned coordinator was restarted for the post-/03 relationship and route correction, followed by a successful frontend reload after compatibility adjustment. The reviewer parent has not loaded those latest backend relationship changes; no normal business service restart occurred. Current observations do not constitute a new frozen candidate.
- Exported package copied here for reproducibility. The browser download remains in Downloads. No external transfer occurred.

## Next checks

Readable relationship editing and stable-ID order/content comparison now have browser evidence. Complete the remaining browser scenarios in the table above. Freeze candidate fingerprints only after the integrated changes settle and rerun the relevant mixed-operation/recovery workload. Existing browser observations do not replace that gate or Windows/colleague acceptance.

## Dated observations and resolved findings, 2026-09-13

The following paragraphs retain the sequence and failures as evidence. Their statements such as “pending” describe that observation's stage; the table above owns current browser coverage.

Both owned HTTP services were stopped and restarted from the recorded isolated launcher after five consistent SQLite snapshots passed integrity checks (`pre-restart/manifest.json`). The receiver imported the work collection through the actual file chooser. It reported validated/received with individual comparison still required. Ana Jokic is an engineering test identity. The receiver reopened PA001, retained the original engineering marker, appended `[ENGINEERING RETURN: receiver revision]`, saved personal revision 3 with Content draft, and exported `review-submission-cc15a65a.zip` (257207 bytes, SHA256 `9e8cce37379397f240eb937df3d93ee815e74ecd52108a7355033578f150c6f5`). Closing/reopening retained revision 3. Its previous confirmation did not survive the edit as active confirmation.

The coordinator imported this saved return package using the actual UI. Submission inbox showed Ana pending. The comparison displayed the full old/new paragraph and the receiver marker, with zero unresolved conflicts. Explicit engineering adoption produced master revision 3 Content draft and the message that content confirmation remains separate. Submission inbox then showed Ana adopted. No real master was changed.

Two remaining UI integration defects were discovered: (1) the left material card still displayed the older personal Content review complete while the open master header correctly showed Content draft; adopted-open did not refresh the queue, and the card did not name which version its state described; (2) the Task results summary omitted the material adoption receipt, but code inspection confirmed the existing Download selected adoption receipts opens a catalogue combining both receipt types. The summary is being aligned; UI export/import of the receipt remains pending. The round trip is not yet PASS.

The initial identical own-submission retry created two records; stable prepare identity is now fixed and exercised by isolated workload checks. Old fixture records remain unchanged. Mixed personal changes and spot-check queue reasons must be identified separately. Source detail now retains same-source queue context, and receiver reopen exposed the pending spot-check link.

Long file-chooser tool delays are automation limitations and are not active mixed-workload stability evidence. Source/Requirement quality, real colleague usability and Windows acceptance are separate gates.

The actual browser also processed TS003 (native page + scanned page + blank page). Extract displayed Partial candidate, four blocks, processed 3 / usable text-table 1 / unprocessed 0 / unresolved 2. Processing details explicitly identified pages 2 and 3, distinguished a visited range from complete accurate extraction, and stated that Requirement identification had not run. Saving retained a personal Content draft (revision 4), and reload/reopen restored all four blocks with 0/3 review checks. The resolved-candidate warning entry disappeared after save, so a retained-evidence UI fix is in progress. Preview original region loaded the preserved page-2 image and Original navigated the reader to page 2; the saved screenshot `../ui/pdf-partial-saved-original.jpg` shows both images. The detector's static missing-src report is therefore also contradicted by actual rendering. Manual transcription was then entered with an explicit engineering/manual attribution, never counted in automatic extraction scores; save completion still requires observation.

After the second owned-service restart, the actual UI reopened TS003 revision 5 with the manual page-2 transcription and attribution preserved, Content draft and 0/3 review checks. Retained extraction limitations now exposes the original processed 3 / usable 1 / unprocessed 0 / unresolved 2 evidence through read-only Processing details. The screenshot is `../ui/pdf-retained-processing-details.jpg`. This verifies recovery and evidence preservation, not automatic transcription.

The updated PA001 queue now explicitly distinguishes Personal content / Content review complete from Master revision 3 / Content draft, and says the accepted archive is retained while the new revision requires review. The receipt selector now names PA001, Ana Jokic, master revision 3 and the original adoption timestamp. The coordinator explicitly selected that receipt and downloaded `adoption-receipts-da7bccaf.zip` (67362 bytes; SHA256 `d41f3de03fb5f4b2ab83f8cc6b9defe5d3c92947d5a67611062837435458b1f4`). Receiver imported it through actual UI; My submissions / Received adoption receipts exposes the correct contributor/adopter and receipt. Package round-trip data transfer and adoption are observed. Receipt presentation still renders the full nested payload, and its import toast incorrectly suggests comparison/adoption for receipt items. These presentation defects keep independent-colleague usability acceptance open.

Actual XLSX scenario: TS002 explicit extraction produced five blocks across Measurements, Empty sheet and Hidden notes; the far Z105 cell and A1:C2 merge were retained. The UI exposed the unavailable C3 formula cache and did not calculate it. Engineering changed B3 from 12 to 12.5, set numbering TEST 1.1, moved table parent to Top level, and recorded an explicit manual-test note. Save succeeded. Re-extraction retained all four edits and opened four unresolved differences (numbering, parent, notes, B3). Each Keep current was explicitly clicked; application required a separate confirmation and displayed “Machine comparison applied to personal work. Master unchanged; review confirmation remains separate.” The final rendered table retains 12.5, TEST 1.1, the manual note, formula text and original associations, with 0/3 review checks. No automatic-extraction score includes these manual edits. Reopen and stale-multiple-tab browser checks remain pending.

Two actual coordinator tabs opened the same saved TS002 revision. Tab 7 saved B3=13; stale tab 8 attempted B3=14. The UI rejected the stale write with “A newer saved revision prevented this write”, retained 14 in the editable field, and exposed Compare saved and local. The actual reconciliation dialog showed saved revision 8/B3=13 beside local B3=14. Explicit Keep my local block plus confirmation opened a combined draft and stated that no server content had been replaced. A separate Save succeeded. Tab 7 Reload saved then rendered B3=14, preserved TEST 1.1, source/formula/merge notes, and 0/3 review checks. `../ui/xlsx-stale-tab-resolved.jpg` records the resolved UI. This is actual-browser staleness recovery, separate from API workload tests.

Service-unavailable save scenario: tab 7 edited the TS002 note with `[SERVICE-FAILURE INPUT MUST SURVIVE]`, then engineering stopped only its owned coordinator service. Save returned “Failed to fetch. Your draft is retained. Retry uses the same request identity after an uncertain response.” The exact edited text remained in the field (`../ui/save-failure-retained-input.jpg`). Restarting the same isolated origin and clicking Save again succeeded with the separate-review reminder.

Final receipt presentation was observed after frontend reload: Received adoption receipts shows PA001 / Ana Jokic / adopted by Weijie Tang / master revision 3 / Content review at receipt Draft. The full payload is hidden under Original receipt details (`../ui/received-adoption-receipt-summary.jpg`). Re-import of the identical receipt file returned “Adoption receipt recorded; your personal work is unchanged. No further adoption is needed.” This closes the misleading success-message and raw-payload presentation findings; actual Windows and real-colleague acceptance remain open.

Additional table-structure browser check: TS002 merged relationship height changed from 2 to 1; an explicit row was inserted at position 2 and labelled ENGINEERING INSERTED ROW. The table block was moved down past the separate far-cell table, then saved. Reload saved rendered far-cell table as block 2 and TEST 1.1 table as block 3, four rows with the inserted row at 2 and Temperature / 14 / preserved formula at 4. Originals and recorded associations remain the comparison source; these deliberate engineering edits are never quality scores.

Post-/03 relationship integration: startup initially remained at Loading requirement workspace because a new top-level JavaScript dependency returned HTTP 404. The isolated coordinator was restarted after the route correction; actual browser startup then exposed Open materials with the persisted engineering reviewer. The final compatibility adjustment moved the helper implementation into an already served module, removing the new asset from the main import graph; another actual frontend reload started successfully without a further parent restart. Tests and the preserved failure are in `../ui/relationship-labels/`.

The browser reopened TS002 saved revision 11 with all manual edits. Explicit Extract produced a pending five-block candidate at saved revision 13, preserving personal content. Compare / resolve opened an inline comparison, not a dialog. Because block order differed, the existing identity-list protection offered one whole Blocks conflict, with nested fields still too technical for business review. The relation values inside it now show Measurements / Top level / Related content with collapsed Reference details. `../ui/relationship-labels/ts002-reordered-comparison.jpg` and `.txt` preserve the observation. No conflict choice or candidate application was made; Return to my work retained B3=14, inserted row, numbering, reordered blocks and the service-failure note, still 0/3 ranges reviewed. A field-level relationship editor was not exercised by this whole-list comparison. The displayed Input revision 6 is a content revision, distinct from saved history revision 13; the number difference alone is not evidence of incorrect-version application, but the labels require clarification.

Versioned comparison browser verification: after the seven-store `pre-merge-v2/manifest.json` checkpoint, TS002 revision 13 opened four separate differences (numbering, parent, table structure, order). Moving Measurements below its child caused an actionable heading-order error; the arrangement dialog retained the attempted sequence (`../ui/relationship-labels/order-comparison/invalid-heading-order.jpg`). Restoring the heading allowed the choice. The parent selector exposed Top level and Measurements and saved Top level. Explicit Keep current numbering/table plus Use candidate order produced zero unresolved differences. The engineering-only application confirmation produced personal revision 14 and explicitly left master/confirmation separate. Reload restored candidate order alongside TEST 1.1, manual 14, inserted row, formula, note and altered merge relationship (`applied-personal-v14.jpg` in the same folder).

The final label check used another explicit reorder/save/re-extract of the same TS002 snapshot, ending at personal revision 17. Both tables now identify their distinct original ranges A1:C3 and Z105:Z105 in the order comparison (`final-readable-order.jpg`). A pre-fix browser failure showed one saved order choice (three unresolved) becoming a new blank comparison (four unresolved) after Return/reopen; `resume-before.txt` preserves it. The new reuse logic was loaded by stopping only owned session 97773 and starting 54642 on the same isolated port 55542. Actual browser reload/reopen restored three unresolved differences and the recorded order choice, despite the retained later blank duplicate; Return/reopen again retained three (`resume-after-restart.txt`, `resume-after-return.txt`). Personal revision 17, manual 14, inserted row and notes remain unchanged; master stays revision 0, review stays 0/3. This is scoped recovery evidence, not a new frozen workload or real business review.

Actual lock recovery: the dedicated `lock_ui_probe.py` held a write-only transaction on TS003 personal storage after an integrity-checked backup. The first non-PTY setup immediately released on stdin EOF (`lock-ui-01`), so it is not UI failure evidence. The PTY-backed `lock-ui-02` held the lock until after the actual Save error. Input including `[ENGINEERING LOCK-RETRY: input retained]` remained visible (`../ui/lock-save-retained.txt`); releasing the lock and retrying saved revision 6, with a separate-confirmation reminder. Reload saved retained the marker and original extraction limitations (`lock-save-retry-success.txt`, `lock-save-reopened.txt`). All prior personal revision payloads are unchanged. The subsequent plain-language error-copy improvement changes no request logic and still needs browser rebinding.

Number-prefix display acceptance: after frontend reload, PA001 archived revision 2 chapter labels show § 13 a, § 22 a and other suffixes once. The saved spot-check finding remains bound to archive revision 2; no full-scope pass or finding resolution was claimed. Evidence: `../ui/relationship-labels/number-prefix/actual-archive-chapters.txt`.

Empty-result recovery: TS004 explicit Extract reported No usable extracted content, one processed range, zero usable text/table ranges and one unresolved range (`../ui/empty-extraction-visible.txt`). A manually entered engineering note was saved as a personal candidate draft and retained on Reload saved and a later browser reopen, with Human Unreviewed attribution and 0/1 review checks (`../ui/empty-extraction-manual-reopened.txt`). The manual note is never an automatic extraction success. Opening the deliberately malformed TS005 PDF subsequently disabled all eight observed material/source navigation controls during the request, returned a PDFium data-format error, retained the TS004 title/note, and restored all eight controls. `../ui/malformed-original-open-recovery.json` records the actual UI observations. This verifies material-open failure recovery, not a failed extraction job or missing-original recovery.
