# Near-Term System2 Local Source Intake Plan

2026-09-07 checkpoint: `CONTINUE`. Minimum intake is complete; extend detailed contracts from evidence.

Verified: source eligibility from current System1 fields; pure template-HTML parsing and legacy comparison; explicit failures; targeted regression in the new environment; a one-page call to the existing PDF pipeline through the adapter.

Next experiment at that checkpoint: inspect tables outside `<p>`, body outside sections and nested lists in additional real HTML windows. Freeze DOM/field evidence contracts before Requirement extraction. Success requires explicit field locations, review for omissions and old-sample regression. New templates require routing changes, not blind reuse of selectors.

PDF accuracy expansion is paused. The lightweight one-page run checked integration only; mixed column text and local pdftotext failures remain in the report. Freeze sheet/cell contracts before Excel implementation. Workbench and writes belong to their owning task.


Later evidence: non-PDF inspection found structural gaps in PA001 section-revision notes and PA002 annex geographic-boundary lists. Prioritise ownership/locations in those real DOM windows instead of repeating clause-count checks. See `../reports/07-nonpdf-module-regression.md`. Initial Excel content parsing requires independent acceptance.


Multi-template checkpoint: 12 eligible HTML samples yielded four supported pages, seven unsupported pages and one empty-body snapshot. Follow `../reports/08-html-template-matrix.md`: first Lovdata tables/annexes, then ASC standard/manual templates. Return PA039 for source-completeness review; navigation cannot replace body text. Repeated-traversal performance is fixed; do not repeat that diagnosis.


The HTML v2 implementation target is now complete across five template families with independent verification. Evidence, upstream intake state and boundaries belong to the [completion report](../reports/09-html-parser-completion.md).
