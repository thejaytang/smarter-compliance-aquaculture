# Requirement Workstream acceptance: rolling plan

Status: SUPERSEDED for current execution by the user-started [stage v2 plan](requirement-workstream-stage-v2.md). The long-term aligned target remains. Checkpoint 47 below records the immediately preceding engineering work and supplies scoped evidence for v2; the clock and active priorities belong solely to the v2 plan.

Read the [aligned target](../design/requirement-workstream-target.md), [decision boundaries](../design/decision-boundaries.md) and [integration state](../../PROJECT_STATE.md). This plan owns current execution; system state files own verified outcomes. Historical pause instructions record prior events; this explicit assignment resumes the necessary in-scope work.

## Checkpoint 47: current-method repair and delivery closure

- Question: does the current original-page verifier still support located correction, affected B invalidation/review, durable history and coherent asynchronous Excel output through the shared workbench?
- Reuse retained isolated development stores and originals. Copy them to new checkpoint-owned paths before any mutation; keep normal databases, source decisions, Canonical, Gold and past evidence unchanged.
- Cover real native-text/complex-table material and existing synthetic scan/cross-page controls. Report evidence classes separately. Synthetic scans do not satisfy the real-scanned-source quality gate.
- Record current source/history/output bindings and run the current verifier, then correct an evidenced discrepancy through the shared browser. Exercise stale checks, retained earlier B history, recheck and current output readback. Reuse earlier verified migration/recovery evidence, checking for drift.
- Success: the original can be located, the correction changes only the effective result, affected B delivery suspends until rechecked, failed/stale submissions do not complete, history survives, and a consistent saved version reaches Excel without blocking ordinary reads/saves.
- Failure: diagnose the smallest reproducible gap, repair the owning component, then repeat the affected interaction. Do not substitute regression count for this closure.

## Following horizon

1. Complete source-family/exposure/reference qualification and a frozen development/calibration/held-out protocol through the shared workbench, retaining unknown independent metrics until actual independent human evidence exists.
2. Address observed A/B errors and representation gaps on development material; preserve configurable provisional subdivision and advisory Requirements.
3. Evaluate natural and injected errors separately, including verifier misses/false alarms, A dimensions, B precision/recall/association/subdivision, automation coverage and actual human effort. Propose justified budgets; user thresholds remain unchanged.
4. Verify weekly 5/20/5 mechanisms, offline operation, recovery and measured performance on declared hardware. Enable no new schedule or external provider without the applicable explicit authorization.
5. Audit every applicable acceptance gate. Keep the goal incomplete while any required evidence/input is missing.

## Required external inputs

- Actual independent reference reviewer/adjudicator and their source/output exposure.
- Authorized real scanned-body and appropriately separated fresh calibration/held-out material. Existing exposed Gold/development pages cannot be relabelled as held-out.
- Peer confirmation of permanent splitting, parent/child delivery and counting. The current reversible policy remains provisional.
- User stage release thresholds; existing defaults are configuration, not demonstrated accuracy. Scheduler/provider authorization applies only if those optional operational paths are activated.

Continue work not dependent on unresolved inputs. No source discovery expansion, full large-PDF run, Gold overwrite, System3 ontology, external transmission, publication or commit is implied.
