# Local PDF reading integration

2026-09-13. **PASS for the bounded browser scenarios below. Full reader fidelity, complete UI acceptance and the active product goal remain open.** The prior browser-plugin experiment displayed an empty region; this change adds an actual PDF.js reader and preserves the image fallback. It does not change extraction, original files, content adoption or Requirement processing.

Subsequent [reader recovery checkpoint](recovery/RESULTS.md) verifies actual refresh/page/zoom and initial-load Retry behavior, with 164 Python / 184 frontend checks. It supersedes only the corresponding open recovery item and regression binding below. The user has deferred further automatic-accuracy work; historical quality failures remain evidence.

## Implemented and loaded

The workbench bundles Mozilla PDF.js 6.3.289 locally with its worker, fonts, CMaps and image resources. The npm tarball SHA-512 was verified, and all 287 selected upstream files have exact hashes in [the vendor manifest](../../../../workbench/ui/vendor/pdfjs/manifest.json). Upstream guidance: [component example](https://github.com/mozilla/pdf.js/tree/master/examples/components), [distribution setup](https://mozilla.github.io/pdf.js/getting_started/). No CDN, upload, model, new business schedule or external asset loading is used.

The updated server advertises the reader only for PDFs. Older parents continue serving the existing page-image path, keeping startup compatible. The new reader consumes the same registered, hash-verified original endpoint. It provides continuous pages, original-position text selection, document search, fit/percentage zoom, rotation, a current-page full-reader link and an explicit Page image fallback. Full-reader pages display the served source filename. Rendering and text/annotation errors stay associated with their page; neighboring successful renders do not clear a visible page's failure. Scan/blank pages state that text selection is unavailable. PDF scripting, XFA, external links and form editing are disabled; the existing original-page renderer remains independent.

Loaded only at the owned fault fixture `http://127.0.0.1:60905/`, current session **20726**. Coordinator 55542 and reviewer 58814 retain their previous parent versions, and normal 62742 was not restarted. A temporary inspection of 60905 in coordinator tab 7 was used because viewport overrides affect the selected tab; its normal 55542 location was restored afterward. No business content adoption occurred.

## Actual browser evidence

- CS010, the preserved four-page public original, personal revision 0, Not extracted: PDF.js displays the published page layout and an original-position text layer. A double click visibly selected “Aquaculture”. Clipboard automation could not provide a reliable copied-text result; **clipboard round-trip acceptance remains UNMEASURED**, not a proven product failure or pass.
- Actual search for `certification` found 8 matches. Next match advanced from 1/8 to 2/8 and page 3; the workbench full-reader link updated to page 3. [Restarted reading](restarted-reading.txt).
- Page image fallback returned to **page 3** without editing or extraction: [return evidence](page-image-return.txt).
- Clicking **Open reader** actually opened a separate local tab at page 3 with the served source filename: [full-reader UI](full-reader-page3.txt), [screenshot](full-reader-page3.jpg). Its appearance is no longer dependent on the failed browser PDF plugin.
- Actual CS010 page-1 captures at [1280](cs010-1280-confirmed.jpg), [1440](cs010-1440-final.jpg), [1920](cs010-1920-final.jpg) have corresponding `*-final.json` files recording the actual CSS viewport and reader geometry. Compare the same page/revision with [the retained pre-correction image reader](../reader-fidelity/cs010-1280-before.png). At narrow three-pane widths, fit-width text remains small; zoom and Open reader are necessary for detailed reading. These are bounded control/layout observations, not whole-application zoom or accessibility acceptance. One attempted viewport override affected tab 7 while tab 11 was inspected; it was excluded and corrected, not counted as a 1440 pass.
- The engineering native/scan/blank three-page fixture shows normal text on page 1, and explicit no-selectable-text status on [scan page 2](scan-page.txt) and [blank page 3](blank-page.txt). This verifies status handling, not automatic OCR or extraction quality.
- After owned session 21251 was stopped, the already loaded CS010 reader navigated to page 2 and retained its page/text. [Offline observation](loaded-document-offline.txt). Search was initiated while offline, but its completed count was observed after restart, so offline search completion is not claimed. Resuming the same fixture as session 20726 retained the active source and permitted continuing search. This is a small loaded-document recovery check, not general offline support or a stability workload.
- No warning/error entries were returned by the browser console observation during the final width checks. This does not prove every PDF type is supported.

## Regression and preservation

Current bound regression: **164/164 Workbench Python tests and 180/180 frontend tests PASS**, with no source drift during [the final run](final-checks/result.json). HTTP tests cover resource MIME/bytes, the narrow viewer CSP, unknown/traversal/foreign-Host rejection and exact upstream file hashes. Reader tests reject stale/foreign-frame page messages, preserve source/view bounds and ensure mode changes issue only the current-page read. Original streaming guards remain unchanged. Earlier test-harness defects (unsupported HTTPConnection context-manager use and an incorrect guessed font filename) were corrected before the passing focused and full runs; they were not product outcomes.

[Six pre-restart store snapshots](pre-restart/manifest.json) passed integrity checks. [Preservation](preservation.json) retained all 15 prior rows across nine checked history tables; nine current original files match their content-addressed identities. These file counts include stored copies, not nine distinct quality samples. Opening the additional engineering fixture created new isolated read state; no claim is made that every live database byte stayed unchanged.

## Recovery and next work

Pre-change `server.py`, `materials.js` and `materials.css` are under `pre-change/`; source hashes for the current integration are in `final-checks/code-before.json`. Resume the preserved fault fixture with the existing Workbench interpreter and `project-support/product-readiness-20260912/serve_isolated.py faults`; never rebuild or restore test stores over business data. If reverting, review only this reader delta against the checkpoint and restart an owned isolated parent. Keep originals, history and dependency provenance.

Remaining: actual clipboard round trip, broader independent complex-PDF fidelity (including unusual fonts, image codecs, annotations and encrypted/XFA cases), standalone initial-load failure recovery, true browser zoom/preference/keyboard coverage, final integrated workload and normal deployment loading. HTML still has the earlier reflow/absent-asset limitations. Extraction quality remains FAIL on the existing reserved diagnostics; Requirement identification remains separate, and Process remains Not connected. This checkpoint closes none of those gates.
