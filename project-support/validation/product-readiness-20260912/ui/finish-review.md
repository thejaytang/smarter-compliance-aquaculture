disposition: fix

Independent Impeccable Operate finish review; code-led refinement against PRODUCT.md and DESIGN.md, with no approved composition image or separate quality-bar card supplied. Desktop scope is the user's 1280/1440/1920 viewport set; browser zoom, keyboard, fullscreen, preference fallbacks and behavior verification remain with the parent agent. The initial review performed no browser or product writes. A separately authorized bounded correction is recorded below; no browser confirmation has yet been supplied, so the disposition remains `fix` pending recapture.

## persistence

PASS: PRODUCT.md and DESIGN.md exist and establish one task and visual authority. Reviewed all six `matched-{before,after}-pa001-{1280,1440,1920}.png` captures, their JSON viewport records, material/source-label rendering, collaboration disclosures/provenance, and shared/component CSS. All captures are valid nonblank matching PA001 saved personal revision 3, query `ENGINEERING CHECK`, same selected source paragraph and document-top workspace state.

Evidence boundary: the before capture serves the immutable baseline frontend against the copied same PA001 personal revision 3 and current read backend. It is a frontend comparison, not an extraction-engine comparison. The after source-original rendering still uses reader backend v7. The files have `.png` names but JPEG bytes; their decoded dimensions are 1280×1000, 1440×1000 and 1912×1000. Both nominal 1920 captures have the same 8 px raster/viewport discrepancy; the capture record states viewport 1920, and the parent must retain this distinction in its browser evidence. The visible engineering markers identify isolated test content rather than a genuine colleague review.

## fidelity

| Element | Verdict | Evidence and boundary |
| --- | --- | --- |
| TYPE | match | Local sans, compact pane titles, dark document text and restrained controls follow the professional document-work direction. No marketing display type or decorative hierarchy. |
| MATERIAL | match | Glass is confined to shell/task control surfaces. Source, editable content and long third-pane text sit on opaque bases. No imitation photographic material or external ornamental asset. |
| GROUND | match | Neutral cool light shell, white reading surfaces and pale selected material agree with DESIGN.md. Selection has outline and text in addition to color. |
| Current material and next action | match | Material/version, personal draft state, Save and separate content confirmation are visible at all three widths. The current after wording accurately distinguishes saved content from no current extraction. |
| Three-pane comparison | adaptation | Existing source/content/Requirements topology is preserved. At 1280 the Extract button moves below the pane heading, an intentional responsive adaptation that keeps controls visible. At wider sizes it sits beside the heading. |
| Package exchange | adaptation | Native disclosure reduces the permanent collaboration toolbar while keeping Source review and My submissions visible. The expanded disclosure explains separate import candidates, explicit adoption and manual file exchange. This follows DESIGN.md task hierarchy. |
| Original position | contradicted | The selector shows source-derived labels, but the reading card still exposes `original-d5cb7f1635c78735`. `materials.js:37` prioritizes opaque anchor IDs; the same helper labels multi-source choices. The user cannot identify the original passage from that string. |
| Processing and review truth | match | Disabled Process and explicit Not connected remain visible; personal changes await adoption, source review and content review are distinct. No extraction-quality or Requirement-completion claim is added. |
| Static readability | match | Main actions and captured content do not overlap or clip. Whitespace/toolbar changes improve usable height at 1280 and 1920; 1440's source starts slightly lower because the location controls wrap, so no universal vertical-space gain is claimed. |

Token-only WCAG contrast calculation using the sRGB relative-luminance formula: main ink/white 14.16:1; muted/white 7.30:1; muted/subtle 6.74:1; muted/selection 6.24:1; white/accent 6.51:1. These support the design-token choice only. Actual composited backgrounds, every state, focus visibility and keyboard reachability are not established by a screenshot or source declaration.

## ceiling

Reached for the requested static desktop refinement after the single semantic location-label correction below. No decorative escalation is warranted. Glass remains subordinate to document comparison; the third pane's honest unavailable state is appropriate to the current product boundary. Dynamic accessibility and workflow acceptance remain UNMEASURED by this reviewer and require the parent's browser evidence.

## material_fixes

1. Replace opaque HTML anchor IDs in ordinary reading/location-choice labels with the matching loaded reader anchor label, or a clear fallback such as `Linked HTML location`; retain exact IDs in the underlying references and expandable provenance. Apply only in presentation, preserve full source lookup and do not invent passage labels. Evidence: every matched after capture and `workbench/ui/materials.js` `locationLabel`, `blockMarkup`, `locateBlock`. **Implemented; browser confirmation pending.** The formatter now uses exact anchor ID, or locator only when no anchor ID is supplied, to find a loaded reader label. Unknown HTML locations use the clear fallback. Reading-card labels refresh after asynchronous reader loading by changing label text only; editor contents and draft objects are not rerendered or mutated. The editor's expandable `Exact source references` retains all original reference values. PDF page and spreadsheet cell labels are unchanged.

Validation after the bounded correction: `node --check workbench/ui/materials.js` passed; `node --test --test-reporter=dot workbench/tests/materials-ui.test.mjs workbench/tests/collaboration-ui.test.mjs` passed all 81 tests. Three added tests check exact lookup versus unknown anchors, unchanged PDF/sheet labels, escaped visible labels versus preserved raw references, and late reader label refresh without draft mutation or content rerender. The first run was 80/81 because the new provenance assertion did not match JSON newlines; fixing that assertion produced 81/81. No test input, source reference or product behavior was changed to satisfy that assertion. Existing matched screenshots remain **before this location-label correction**, including files named `matched-after`; they cannot confirm the correction visually.

## keep

Keep opaque reading surfaces, visible Save/version context, explicit saved/adopted/reviewed distinctions, complete original navigation, native disclosures, and the disabled unconnected Requirement processor while making the bounded correction.

## Detector adjudication and browser evidence limits

The concentrated pass ran Impeccable detect once (`impeccable-detector.json`). The side-border finding is the retained prior-human-review panel, not a repeated decorative card treatment; it is accompanied by an explicit prior-review label. Retained with no decorative change. The source-preview image finding is a static false positive: the adjacent code assigns the registered page image to `img.src` after verifying a returned image and a valid region. No placeholder asset was introduced. The actual source preview still requires browser evidence.

Coordinator browser checks observed expanded workspace entry/exit and keyboard resizing: the original pane changed from 344.953px to 364.953px with ArrowRight, with a visible 3px focus outline, then Reset column widths restored layout. Matched post-change captures show no horizontal document overflow at actual DOM widths 1280, 1440 and 1920. A 960px viewport reflow was also inspected; it is not browser zoom. Attempted zoom shortcuts did not change DOM width, DPR or viewport scale, so actual browser zoom remains UNMEASURED. Reduced-motion/transparency media fallbacks exist in CSS; browser preference toggling has not been observed. The DOM contrast check of visible enabled text controls found no sampled ratio below 4.5, but excludes iframe text and complex backgrounds and is not an all-state accessibility PASS.

## Known relationship-comparison gap, frozen run03

Read-only follow-up, 2026-09-13. The parent observed TS002 re-extraction comparison showing `Content · Parent Id`, an empty current value, and candidate `block-c348…`; the referenced candidate heading is `Measurements`. This reviewer verified the responsible code, not the browser state. The problem materially affects choosing which hierarchy to retain, rather than visual polish. No product edit or new validation run was performed during the freeze.

`collaboration.js::differenceLabel` turns the final path component into `Parent Id`; `differenceMarkup` gives the raw scalar/array to `valueMarkup`. The same problem applies to `dependencies`, including individual dependency-array indices. `Edit result` also presents those identifiers as generic text fields, so manual relationship correction requires knowing internal IDs. Whole-block addition/deletion or structural `/blocks` differences can expose their nested `id`, `parent_id` and `dependencies` through the generic object renderer; this is an equivalent readability limitation, but not authority to redesign arbitrary schema rendering.

Important data boundary: `Collaboration.merge` does **not** currently receive separate `current` and `proposed` documents. `collaboration.py::merge_view` has exact `base`, `current` and `proposed` blocks locally, but returns the combined `material`, raw per-side difference values and provenance. `showPreview` replaces the working draft with that combined material. Resolving both sides against its blocks can mislabel a renamed heading, a removed block or a candidate-only parent. A title from another side must not be used as if it described this side.

Smallest accurate correction for a future candidate: derive a compact, read-only block-reference display map inside `merge_view` from each exact side, limited to the referenced IDs needed by parent/dependency differences (including references in whole-block differences). Supply it alongside the existing comparison response, without changing persisted materials, decision values, difference IDs or merge rules. The renderer can label the fields `Under heading` and `Related content`, show exact-side numbering plus heading/text, and retain raw references in closed details. Explicit null parent means `Top level`; an empty dependency list means `No related content`; absent fields still mean `Not present`. A missing or invalid target must say `Referenced heading/content not found` and expose its exact ID in details, without guessing or implying validity. Escape every label. A title-less but existing target can use its real block type and source location as a fallback; it must not borrow another block's title.

For this narrow change, keep/adopt decisions remain the existing value-preserving controls. If manual relationship editing is included, replace only the parent/dependency editor with a located block chooser bound to actual IDs, with heading-only parent targets and existing backend validation; do not present a free-text ID as a completed business-user solution. This is a follow-up scope decision, not implemented here.

Required targeted checks before claiming the gap resolved: same ID with different titles on the two sides; candidate-only and removed targets; null versus missing versus empty arrays; unresolved ID; escaped hostile title text; dependencies and whole-block references; unchanged request IDs, selected stored values and conflict/replay behavior. Then reproduce the TS002 hierarchy decision in the browser. The original finding and frozen run03 remain historical evidence; the authorized post-freeze implementation below awaits browser confirmation.

### Bounded implementation and isolated checks

Implemented `collaboration_relationships.py` and `collaboration-relationships.js`, connected through the existing `collaboration.py` and `collaboration.js`. The preview response now derives a small `reference_context` for base/current/incoming from their own referenced blocks. Ordinary relationship differences and relationships inside whole-block differences use that side's heading/content text; unknown targets stay explicit and raw references remain in closed details. This is derived presentation context, not a persisted material/Requirement schema change.

Parent editing selects among actual preceding headings in the combined content, respecting heading levels. Related-content editing uses a multiple-selection control and an explicit Clear action. Values sent to the existing resolve endpoint remain the exact IDs (or null/empty list), never labels. Missing/removed targets and a changed comparison keep the dialog and user selection open with an actionable error; no block is created or restored. Backend guards check explicitly selected relationships before either coordinator or machine-comparison decisions are persisted, including missing targets, parent position/level and circular dependencies. Reading historical comparisons is still allowed even if an older saved choice is invalid, so the person can reopen and repair it. Final material application retains the owning store's existing complete validation. Other field editors and merge rules are unchanged.

Final isolated validation: **41/41 backend collaboration tests and 97/97 frontend tests passed**, including exact-side rename differences, unavailable target handling, escaping, reference preservation, null/missing/empty distinctions, real temporary-journal coordinator and machine resolve paths, no journal write on rejected choices, selected-ID request payloads, busy-operation guards and preserved inputs after server rejection. Logs: [backend-tests.txt](relationship-labels/backend-tests.txt), [frontend-tests.txt](relationship-labels/frontend-tests.txt). The final frontend run took 39.564 seconds during a shared environment/file-I/O delay; all existing test and manifest processes subsequently exited successfully. No extra retry run, global environment repair or service restart was used after that delay was identified.

Recovery evidence: exact pre-edit bytes of both changed existing product files, plus the unchanged existing frontend test file, are under [pre-change](relationship-labels/pre-change/sha256.json). New files and final hashes are listed in [implementation-sha256.json](relationship-labels/implementation-sha256.json). Restore only those two product files and remove the two new runtime helpers if deliberately returning to the previous checkpoint; do not overwrite unrelated later changes. Original input, candidate payloads, historical decisions and business runtime data were not edited.

Loading/acceptance: this subtask did **not** restart or navigate a browser. Restart the Workbench Python service for each isolated coordinator/reviewer instance that will use the shared changed code, then reload its frontend modules. System1 and System2 code/environments were not changed by this fix and need no component-specific restart for it. Reopen the retained TS002 comparison and verify readable Current/Machine/Base labels, actual heading/dependency choices, unknown-target error recovery and selected-ID preservation. **Implementation and isolated checks complete; browser acceptance still pending.**

### Startup regression found during parent integration

The parent restart exposed a real integration regression: the workspace remained at its loading shell despite `/api/state` and `/app.js` returning 200. A read-only HTTP probe confirmed that `/collaboration-relationships.js` returned **404 JSON**. The new module had not been added to `server.py`'s explicit static route map, breaking the top-level `app.js → materials.js → collaboration.js` import chain. The earlier Node tests imported files directly and therefore did not cover this boundary; they did not establish that the workspace could start through HTTP.

The bounded correction adds that one static route entry. Exact `server.py` pre-edit bytes/hash were preserved alongside the earlier recovery files. A new actual-HTTP test follows the local static and dynamic JavaScript import graph from `app.js`, checking status 200, JavaScript MIME and exact response/source bytes. It reproduced the missing-module 404 before the correction and passed after it (1 test, 1.655 seconds). Evidence: [probe](relationship-labels/startup-routing-probe.json), [before failure](relationship-labels/http-module-before.txt), [after pass](relationship-labels/http-module-after.txt). The final implementation manifest includes the route and test files. This did not change schema, data or business processing; no service restart or browser operation was performed by this subtask. The parent must restart the Workbench service with the route correction and confirm actual workspace startup and relationship behavior before closing the UI finding.

### Compatibility correction for parents already running

A further integration boundary matters: an older running Python parent serves updated frontend files from disk but retains its old static allowlist. Adding a route in source does not update that process. To avoid introducing a new HTTP dependency into the normal workspace, the relationship implementation now lives only inside the established `collaboration.js` asset. `collaboration-relationships.js` is a thin compatibility re-export, with no duplicate implementation; the application import graph does not request it. This JavaScript-only correction needs a frontend reload, not another Python restart. No normal workspace restart or browser operation was performed by this subtask.

Exact pre-compatibility bytes and hashes are in [before-compatibility](relationship-labels/before-compatibility/sha256.json). Updated tests import the helpers from `collaboration.js`: [97/97 frontend tests passed](relationship-labels/compatibility-frontend-tests.txt). The [HTTP import-graph test passed](relationship-labels/compatibility-http-tests.txt), checking exact bytes and JavaScript MIME and explicitly asserting that the app does not depend on `/collaboration-relationships.js`. The earlier missing-route failure remains preserved, not relabeled as a successful startup check.

### TS002 reordered-list comparison and input revision, read-only diagnosis

The parent reports that startup now succeeds and TS002 retained its manual value `14`, inserted table row, reorder and note. It was saved at revision 11; extraction start/result advanced the record to revision 13, with the candidate still pending and no comparison choices applied. Parent-captured evidence is `relationship-labels/ts002-reordered-comparison.*`. This reviewer inspected the responsible code, not the browser.

The single `Content · Blocks` conflict is consistent with the existing `collaboration_merge.py::identity_list` guard. A changed relative order of retained base IDs, simultaneous removals/additions, or differing same-gap insertions switches to one whole-list structural difference **before** per-ID field comparison. Either branch can trigger it. Because the extraction baseline copies the current saved blocks, a fresh parser restoring source order can differ from a manually reordered baseline even when the personal list has not changed again after extraction. Stable IDs alone do not bypass that guard. `machine` preview then requires an explicit decision while retaining current content by default. No evidence here shows a lost table repair or silent overwrite.

The relationship label fix operates inside this whole-block-list display, so `Measurements`/`Top level` can appear correctly without a separate `/blocks/<id>/parent_id` difference. The focused relation chooser is only invoked for such a granular field difference; the current whole-list `Edit result` still uses the generic structured editor and exposes internal fields. This remains a core usability gap for selective recombination. A narrow parent-only comparison can verify the new chooser independently, but cannot stand in for acceptance of the reordered-list scenario.

Smallest next step without changing merge semantics: explain the structural conflict as an **order/content list change**, show ordered block titles/text with source details folded, and make the all-or-nothing effects explicit (`Keep current content and order` versus replacing the full list with the candidate). Preserve the existing safe keep-and-continue path. That improves decision clarity but does not establish selective merge capability. A separate bounded algorithm experiment could later split order from per-block fields **only after exact stable-ID correspondence is verified**, keeping structural fallback for unknown split/merge, delete/edit, type/table-shape changes and ambiguous insertions. It would require an explicit comparison-contract/version strategy for already-saved decisions plus regression of human table repairs, headings/dependencies, competing orders and replay identities. Do not silently change historical `/blocks` decision meaning. No merge-algorithm change was made here.

`Input revision 6` is bound to `material.content_revision`, not the saved record's `material.revision`. In `start_candidate`, the former is copied to `candidate.input_revision` with `base_blocks` and source; extraction start and finish increment only the latter. `_edit` increments the content counter only when blocks/issues change. Thus saved 11 → 13 alongside input content revision 6 is consistent with two extraction lifecycle records and is not evidence of the wrong input. Actual application additionally compares current material and candidate fingerprints, then uses the current saved revision guard. The UI should say **Input content revision 6**, and distinguish current saved revision 13 where shown; it must not invent an input saved-revision field that the candidate did not record. This is a version-label clarity gap, not a demonstrated binding failure.

## Current-candidate browser confirmation

`matched-final-pa001-{1280,1440,1920}.jpg` and `matched-final.json` now capture the unchanged receiver PA001 personal revision 3, the same ENGINEERING CHECK paragraph and selected original, with actual viewport width checked and scrollY=0. All three have no horizontal document overflow. The ordinary original-location label is now readable source text, verified through DOM and screenshots, replacing opaque original-* IDs. These are the latest after images to compare with the preserved matched-before images. The receiver's stored shared master snapshot remains revision 2; receiving an adoption receipt does not synchronize that snapshot or overwrite its personal revision 3. The coordinator separately adopted revision 3. This is an offline exchange, not live synchronization.

The primary controls and reading/editor surfaces were visually checked at all three widths. At 1440 the non-operational Future connection boundary summary still truncates at the reserved-pane edge; this is retained as a minor UI gap rather than declaring complete all-state visual acceptance. Actual browser zoom and reduced-preference validation remain UNMEASURED. No further decorative pass was started.


### Version 2 order comparison: implemented, targeted frontend checks complete

The prior TS002 reordered-list usability finding now has a bounded UI implementation for the parent's versioned merge contract. A `/blocks` difference with `kind=order` displays each side's own ordered titles, numbering and recorded original page references. Exact IDs remain in closed reference details. `Keep current order`, `Use candidate order` (or submitted), and `Arrange order` distinguish the action. The editor starts with the actual combined blocks, uses keyboard-operable Move up/down buttons with retained focus, and sends only a complete permutation of IDs through the existing resolve endpoint. It cannot create, delete or replace block text/table content. Missing version or incomplete side correspondence prevents this editor from opening. Backend hierarchy validation is owned and measured by the parent, not claimed from these UI tests.

Historical or ambiguous whole-list differences remain whole-list choices. The default display now shows full text and readable cell grids, with internal source fields and original stored details folded. Whole-table differences, including machine-forced review, likewise expose readable cells and clearly state that the action keeps or replaces the entire table. Individual corrections direct the user back to the material editor after keeping and applying the reviewed choice; these fallback paths no longer open the generic internal-schema editor. Cell grids preserve all stored cell values; merged regions are disclosed separately and their layout still needs original comparison. Existing ordinary field/cell editing remains unchanged.

The candidate summary now labels `Input content revision` separately from the actual `Current saved record` revision. It does not infer an input saved-record number. There is no new JavaScript asset dependency, runtime restart, browser operation or business-data mutation in this subtask.

Validation: [105/105 targeted frontend tests PASS](relationship-labels/order-comparison/frontend-tests.txt), including eight new order tests: side-specific title changes and escaping; readable structural/table fallback; strict permutations and immutable moves; exact resolve identity and ID-only payload; historical/missing-input refusal; rejected/stale/missing-content/busy draft preservation; duplicate save suppression; distinct version labels. The actual HTTP graph and Python integration are being validated separately by the parent team. Before-edit bytes/hash are in [order pre-change](relationship-labels/order-comparison/pre-change/sha256.json); changed frontend/test hashes are [frontend-sha256.json](relationship-labels/order-comparison/frontend-sha256.json). The prior TS002 comparison screenshots represent the pre-fix whole-list UI. Actual version 2 browser confirmation and the final mixed-operation stability gate remain for the parent; no screenshot has been relabeled as post-fix evidence.


Parent browser follow-up found that two valid textless XLSX table containers both displayed `table (text not recorded)`, obscuring which item was moving. The bounded correction now reuses exact source sheet/cell range in the common label for order comparison, order editing and whole-table comparison: e.g. `TEST1.1 · Table · Measurements · A1:C3` versus `Table · Measurements · Z105:Z105`. Existing numbering remains, each side uses its own references, and HTML-only locations remain `Linked original location` without exposing opaque anchors. Legitimate textless containers no longer imply missing content. [106/106 targeted frontend tests PASS](relationship-labels/order-comparison/frontend-location-label-tests.txt), including the new source-label test with different sheets/ranges, escaping, preserved input and nonopaque HTML fallback. [Pre-edit recovery](relationship-labels/order-comparison/before-location-labels/sha256.json) and [final frontend hashes](relationship-labels/order-comparison/frontend-location-label-sha256.json) preserve this follow-up separately. Product code is stable; parent browser reload and the related HTTP-graph rebinding remain separately owned checks.


### Existing source-number display prefixes

The parent's PA001 archive revision 2 spot-check found `§ 13 § 13 a.Fellesansvar...` in Chapters. This combines a historically partial parsed numbering field with exact source text that already contains that prefix. A small shared display-only `numberedText` helper now joins the two only when the source does not already start with that numbering at a safe token boundary. It preserves exact source whitespace/text and never edits the stored numbering, source text or draft. It handles `§ 13` / `§ 13 a...` and compact `§13a...`; different human numbering such as `TEST1.1`, `1` versus `10` or `1.2`, and `§ 13` versus `§ 130` remains visible.

The existing `collaboration.js` exports the helper and the already established `materials.js` import consumes it. Chapters, reading blocks, parent/related options, original-linked results, candidate/concurrent-choice labels and relationship/order/difference labels now share this rule. Editable raw numbering and text fields remain independent and unchanged. Historical parser correctness is not claimed by this display fix; parent owns that separate repair.

[110/110 targeted frontend tests PASS](relationship-labels/number-prefix/frontend-tests.txt), including exact/partial-letter prefixes, symbol and whitespace boundaries, longer numeric tokens, distinct human numbering, escaping, unchanged source/draft objects, and actual chapter/reading/parent option rendering. [Pre-edit bytes/hashes](relationship-labels/number-prefix/pre-change/sha256.json) and [final frontend hashes](relationship-labels/number-prefix/frontend-sha256.json) are retained. No new static asset, Python change, runtime restart or browser action was made by this subtask. Parent owns actual archived-PA001 reload confirmation and the team owns the final related HTTP graph binding.


### Navigation availability during delayed operations

Parent fault testing found that opening another material was safely guarded, but some unopened-source rows and spot-check buttons still appeared enabled. `updateNavigationLock` now updates all material/source/inspection open controls and the source selector during `busy` or `opening`. It is called both by `updateBar` and after material-list rendering, so replacement DOM rows cannot briefly become available while a selected material is still loading. Each node's prior disabled state is retained in a WeakMap and restored after the operation, preserving existing permission-disabled controls. No action guard, save behavior or permission rule changed. The parent's database-lock explanation and safe-retry copy were preserved.

[113/113 targeted frontend tests PASS](relationship-labels/navigation-lock/frontend-tests.txt), including three new delayed-operation cases: all open types locked with duplicate opens blocked; a list redraw during loading stays locked; read failure restores previous context and availability while retaining pre-existing disabled controls. [Pre-edit bytes/hash](relationship-labels/navigation-lock/pre-change/sha256.json) and [final frontend/test hashes](relationship-labels/navigation-lock/frontend-sha256.json) are retained. No new asset/Python change or browser action was made here; parent owns actual UI reload/fault confirmation and the team owns the updated HTTP graph binding.


### Original reader correction, pending matched browser confirmation

Parent actual-browser evidence in `reader-fidelity/` superseded the earlier reader-layout assumptions: at 1280×720 the CS010 PDF page started below the viewport after a 123px pane header, 173px toolbar and 104px notice; another PDF showed a 675px raster stretched to 1200 CSS pixels at 200% on DPR 2. The following frontend correction began only after the /6 full regression terminated and its bindings were preserved.

PDF/HTML evidence now owns the remaining pane height. The outer document reader does not scroll; the PDF image viewport or sandboxed HTML iframe owns document scrolling. The original header and page controls are compact, with a short footer status; detailed limitations, selectable unverified native text and the optional browser PDF viewer moved to Reading details. Open original and Linked content remain available. HTML explicitly says `Offline HTML · reflowed reading view`; `/9` source-display warnings remain in its details. This UI does not claim native website fidelity or full native PDF-reader functionality.

PDF detail requests use the measured displayed image width multiplied by device pixel ratio, normalized to 64px and clamped to the backend's 32768 requested-width limit. Zoom, element resize, browser resize and DPR changes debounce for 220ms; one request runs for the current reader at a time. Only the existing read endpoint is called. The old page remains displayed while fetching and decoding. Material, page, source hash, view, reader ticket, session and image element must still match before replacing the image; actual decoded dimensions must match the returned `image_width`/`image_height`. Current user scroll is preserved. Backend-limited results say that available page detail was reached. Old backends ignoring `render_width`, failed reads and invalid decoded responses retain the page, stop automatic retries for the session, and expose Retry/Open original without developer instructions. Cleanup disconnects observers and invalidates late responses. The exact known PDFium data-format open failure now gives source-recovery guidance while preserving other diagnostics and the previous draft.

[171/171 full frontend tests PASS](reader-fidelity/reader-correction/all-frontend-tests.txt), including twelve new checks for physical widths/DPR/limits, delayed replacement and live scroll, stale material/page/view/source responses, network/decode mismatch preservation, old-backend refresh-loop prevention, bounded backend results, debounce/single in-flight request, observer cleanup, retained secondary reading information, viewport layout contract, explicit Retry and malformed-PDF recovery. This is frontend behavior evidence; Python/PDF image generation and actual browser geometry belong to the parent's separately bound checks. [Pre-edit bytes/hashes](reader-fidelity/reader-correction/pre-change/sha256.json) and [frontend final hashes](reader-fidelity/reader-correction/frontend-sha256.json) are retained. The prior `cs010-1280-before.*` captures remain before evidence. No browser operation or service restart was performed by this subtask. Parent should verify page visibility at 1280×720, matched 200% actual raster/display sizes and scroll retention, HTML anchor positioning, zoom/resize/DPR behavior and Retry on the owned isolated service before accepting this correction.


First actual reader confirmation showed a remaining CSS inheritance defect: global `header` supplied 22px bottom margin, and the document-toolbar button rule overrode the intended secondary-button padding/height. The parent's computed dimensions were primary 278×65px and secondary 278×68px; the three secondary controls plus gaps exceeded available width. One bounded CSS batch now sets original-header margin to zero, gives the secondary-button rule sufficient specificity for 26px minimum height and 2px/4px padding, and reduces the secondary gap to 6px. No JavaScript logic changed. [12/12 reader checks PASS](reader-fidelity/reader-correction/toolbar-spacing/tests.txt), with explicit inheritance/specificity regression checks. [Pre-change](reader-fidelity/reader-correction/toolbar-spacing/pre-change/sha256.json) and [updated hashes](reader-fidelity/reader-correction/toolbar-spacing/sha256.json) are retained. Available reader-height improvement remains for the parent's same-viewport measurement; this subtask does not substitute a width calculation for browser evidence.


### Explicit editing-control label associations

Parent reviewer browser testing found that the visible Original text field was not discoverable using exact role/label lookup. The existing template used implicit wrapping labels, which are valid HTML; absence of id alone does not prove the browser accessibility failure's cause. The bounded correction adds explicit stable per-block id/for associations for Type, Original number, Level (headings), Under heading, Original range, Location detail and Related blocks. The main textarea additionally has an explicit accessible name matching its visible label: Original text or Heading text. IDs are encoded from stable block IDs and remain stable across reordering; data-field binding and stored values remain unchanged. Image/table subsidiary editors were not broadened in this correction.

[80/80 focused frontend tests PASS](editor-accessibility/frontend-tests.txt), covering new explicit unique associations, escaping, stable IDs after reorder, exact original/heading names, unchanged initial stored fields and existing text/location edits, plus material/number-label/reader regression. [Pre-change recovery](editor-accessibility/pre-change/sha256.json) and [final hashes](editor-accessibility/sha256.json) are retained. No reader logic, CSS, store or service changed. Actual assistive-name/keyboard confirmation remains the parent's browser check after frontend reload; these template/data-binding tests do not claim screen-reader or colleague acceptance.
