Historical pause checkpoint.

The user requested root-directory repair before further feature work. The unfinished confirmation-dialog change and its test are retained here. materials.js was restored exactly to the preceding verified bytes. The focused run had 3 passes and 1 test-fixture failure (an uncreated mock checkbox); no new dialog was loaded or accepted in the browser. Resume only after directory repair, by reviewing the retained delta against current source.

After the initial cleanup, the candidate and corrected mock-checkbox fixture were restored. The resumed focused run passed 4/4 tests (resumed-focused.log). The subsequent root documentation/launcher consolidation did not alter this candidate. Later actual-browser verification and the resulting count/declaration fixes are recorded in [RESULTS.md](RESULTS.md); this file preserves the interruption history only.
