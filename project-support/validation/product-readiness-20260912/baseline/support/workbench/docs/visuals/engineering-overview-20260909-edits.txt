CONNECTOR CORRECTION PASS 1

Use case: precise-object-edit.
Edit the attached engineering infographic. Preserve all existing wording, sections, colors, icons, card interiors, and general layout. Change ONLY the inter-system and outer-loop connector topology, with minimal added gutter space if needed. Make these precise fixes:
1. DELETE the blue horizontal arrow currently leaving System1's bottom "Workbook / Data own source state" box and entering System2's "Versioned incremental delivery". This is a factual error.
2. Instead draw one clear solid blue orthogonal connector from the RIGHT EDGE of System1's "Effective INCLUDE" box. Route it UP through the empty vertical gutter BETWEEN System1 and System2, then RIGHT into the LEFT EDGE of System2's TOP box "Explicit Start → identity / hash checks → format routing". Arrowhead MUST end at Explicit Start, never at Canonical or delivery. Label in gutter "source ID / snapshot / hash", with compact stacked lines, no overlapping borders or text.
3. DELETE the extraneous purple dashed horizontal arrow going from System2 TOP intake row directly into System3's "Preserve ID, source text & evidence". No top shortcut is allowed.
4. The only System2-to-System3 flow must START at RIGHT EDGE of System2's BOTTOM "Versioned incremental delivery". Keep the short solid blue segment labeled "system3-input/1 / Feed implemented" then draw a dashed purple line up in the gutter BETWEEN System2 and System3, ending with a rightward arrow into LEFT EDGE of System3's TOP "Preserve ID, source text & evidence" box. DELETE the current wrong arrow into the bottom "Semantic service not connected" status note.
5. Fix outer-loop evidence feedback. DELETE the left exterior arrow that currently points down at System1. Instead draw one dashed purple line from LEFT EDGE of the bottom SHARED WORKBENCH strip, routed up in OUTER LEFT MARGIN, ending with an arrow INTO "Observed failures & QA" at top. Label "Error evidence" in left margin. This connects workbench QA to engineering feedback.
6. Keep a separate dashed "Reviewed code / rule updates" output from the top Human checkpoint to the top boundary of the implemented System1/System2 processing area (a bracket across both is fine). Do not point it solely at System3. The outer layer remains marked PROPOSED.
7. In the bottom human loop, extend the "Reopen / report an issue" return connector so it visibly STARTS at Review history and ENDS at Pending review. It must not be an unattached short line.
8. Other-formats human parsing-copy note should point only LEFT back into format routing; remove its downward arrow into PDF.
All other internal arrows and exact text must remain intact. Never cross card text or unrelated nodes. It is acceptable to widen gutters and exterior margins slightly to fit correct routing. Maintain crisp readable text at best available resolution. This is a single targeted connector-correction pass, not a redesign.

CONNECTOR CORRECTION PASS 2

Edit this exact infographic. Preserve everything except THREE tiny connection fixes. Do not redesign or change any body text.
FIX A: At the top of PDF lane, currently its small blue downward input arrow comes out of the "Other formats → Human parsing copy" box. Erase ONLY that short arrow. Replace it with a blue route from the bottom-right part of the long "Explicit Start → identity / hash checks → format routing" box: exit bottom at its right side, travel a little down in the narrow white gap above the format lanes, then right, then down into the PDF heading. PDF must receive the same main intake as HTML and XLSX. The human parsing-copy box should ONLY have its existing LEFT-pointing arrow into Explicit Start.
FIX B: Erase the dashed purple OUTER LEFT connector and its "Error evidence" label that runs from the top edge of System1 up to Observed failures. Replace with a compact label directly below the top "Observed failures & QA" box inside the outer strip: "Input: workbench issues + weekly QA". This text label replaces the long exterior feedback wire. Leave the internal top-loop "Repeat from evidence" connector intact.
FIX C: Erase the dashed purple OUTER RIGHT loop running from the top engineering strip down onto the System3 top border, and erase the label "Reviewed code / rule updates" beside it. Replace with a compact label directly BELOW the top "Human checkpoint" box: "Output: reviewed code / rule updates". This text label replaces the long exterior update wire. Keep the DESIGN ONLY box and System3 dashed border unchanged.
The important long solid blue Effective INCLUDE → Explicit Start connection is already CORRECT, preserve it exactly. The dashed delivery → System3 top preserve-evidence connection is already CORRECT, preserve it exactly. Preserve the corrected reopen/history loop.
These three fixes remove misleading wires without adding crowded cross-panel connectors. Keep the same canvas, legible text, all existing wording and every other diagram element unchanged.
