# Unified source-bound groups

Implement the approved source-selection editor with recursive field groups, structural clause groups, exact/range QC and independent group NOT. Shared conditions remain on the enclosing clause; subject/action/object associations remain in child clauses. Subrequirements reference actual Requirement identities. Preserve source text, Unicode anchors, saved histories, actor isolation, explicit saves and the non-executable interpretation boundary.

1. Add a versioned source-bound tree alongside unchanged legacy units. Read legacy fields/relationships losslessly and persist new trees only after explicit editing/save. Validate counts, source anchors, ownership, references and cycles. Index tree nodes relationally.
2. Replace the default field forms with source selection, compact contextual field/Group tools and recursive inline groups. Keep coloured source previews, QC, NOT, removal, grouping and JSON inspection accessible.
3. Pass authoritative trees through interpretation context, source projections, annotations and full-workspace delivery. Never flatten scopes or silently infer unresolved QC.
4. Validate isolated persistence/restore/export, interaction and source fidelity. Back up before normal-service activation; verify browser workflows, preserve user drafts, and synchronize the authorized branch/PR.

Implementation and local verification are complete; [RESULTS.md](RESULTS.md) owns the evidence and platform boundaries.
