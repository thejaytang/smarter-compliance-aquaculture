# Fourth-pane generation and individual approval, 2026-09-21

## Result

The user confirmed optional API model assistance and requested one button for all three cards. The selected fourth pane now offers **Generate AI candidates**, which requests Scope, Condition and Demand together, including support notes and proposed concepts. It does not run on navigation. Each result remains separate until its individual **Approve … candidate** copies that card into the editable draft. Manual editing works without a provider. Program-combined third-column annotations remain a separate deterministic projection, retaining words, groups and quantities without semantic inference.

Manually authored and earlier saved drafts have individual approval actions as well. Explicit save records those approvals with server-bound reviewer/time and optional candidate run identity. Saving ordinary text never grants approval. Edits to a card or its notes invalidate that card; changes to shared context invalidate all affected approvals. Confirm interpretation requires three saved, current approvals plus the existing completeness/concept checks. The saved-history trail prevents previously adopted candidates from reappearing after human corrections. No approve-all control was added.

## Verification

- The full Workbench suite passed 337 tests. After the candidate-history follow-up, all 37 interpretation/HTTP/continuity tests passed again. The final frontend suite passed 385 tests. See the test logs here.
- A real browser against an isolated synthetic workspace and local mock provider exercised one-button generation of all three cards. Generation left the existing draft and saved revision untouched. Approving Scope alone changed only Scope and its support notes. Approving Condition and Demand, saving, reloading and restarting retained three individually recorded approvals, all tracing to the same actual mock-provider run.
- Editing Condition invalidated only Condition. The page required renewed approval; a separate concept-meaning confirmation and final interpretation confirmation were exercised. The final synthetic interpretation is revision 4 with four retained history revisions. Earlier model candidates stayed retired after the human correction. See `synthetic-approved.json` and `synthetic-final-readback.json`.
- Two isolated services passed package import, receiver re-export and restart. Saved text, designs, named approval records and candidate-retirement history were preserved for another reviewer. Import rebound the Requirement from revision 1 to 2, so active approval status correctly became pending against the changed context. The initial test incorrectly expected current approval after rebinding; inspecting the exact revisions showed the existing source-version guard was working. The corrected check explicitly requires renewed review. See `exchange-result.json` and `receiver-readback.json`.
- Final live-browser readback showed R1, the single disabled generation button, `AI: Not connected`, three unapproved drafts, and disabled final confirmation. Expanding the program-combined Scope showed the three original annotated Subjects with their grouping, separate from the earlier English interpretation. Desktop visual inspection showed readable wrapping in the narrow fourth pane; the targeted design detector reported no findings.
- The live service restarted at http://127.0.0.1:62742/ after a coordinated recovery export and stopped integrity verification. The permanent Example's fields, rule design, revision and history are unchanged. System1 and both System3 database files remain byte-identical. System2 added only the expected `sync_export` receipt for recovery package `47bb98ff-12ba-4e12-9562-b23bbc0a20ae`; all preceding rows are unchanged. See the backup, integrity and preservation records.

## Boundaries

No real remote model call, Site Model execution, native Windows acceptance, commit, push or publication. The real Example remains for the user's human review; no legal approval was performed on it. The local provider and synthetic services/tabs were stopped after verification. Existing source HTML bytes, unrelated edits and histories were retained.
