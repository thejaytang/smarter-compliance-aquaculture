from pathlib import Path
from html.parser import HTMLParser
import json,sys,uuid,re
ROOT=Path(__file__).resolve().parents[2];OUT=Path(__file__).parent
sys.path.insert(0,'/private/tmp');from example_http import call
receipt=json.loads((OUT/'source-receipt.json').read_text());material=receipt['material'].get('material',receipt['material']);mid=material['id']
p=OUT/'population.json';saved=json.loads(p.read_text()) if p.exists() else {}
def record(k,v):saved[k]=v;p.write_text(json.dumps(saved,ensure_ascii=False,indent=2));return v
def ident(key):return str(uuid.uuid5(uuid.NAMESPACE_URL,'adf10-separate/'+mid+'/'+key))
def once(key,path,request):
 if key not in saved:record(key,call(path,request))
 return saved[key]
current=call('/api/material?id='+mid);current=current.get('material',current)
blocks=current['blocks']
specs=[{'key': 'R1', 'ids': [0], 'fields': {'Subject': 'the operational journal', 'Modal Verb': 'shall', 'Main Verb': 'contain', 'Object': 'updated information'}, 'conditions': ['For aquaculture establishments that are not subject to specific record-keeping provisions in Chapters 4, 5 and 6']}, {'key': 'R2', 'ids': [1], 'fields': {'Object': 'aquaculture animals and aquaculture animal products brought into and removed from the aquaculture establishment'}, 'conditions': [], 'details': ['place of origin', 'place of destination'], 'context_requirement': 'R1'}, {'key': 'R3', 'ids': [2], 'fields': {'Object': 'mortality per production unit'}, 'conditions': ['relevant to the production method'], 'context_requirement': 'R1'}, {'key': 'R4', 'ids': [3], 'fields': {'Object': 'results of completed health inspections'}, 'conditions': [], 'details': ['number of completed health inspections', 'sampling', 'examinations performed', 'diagnoses', 'treatments carried out'], 'context_requirement': 'R1'}]
units={'R1':'b2fe01c6-f419-56ec-bdb6-cd2ff4b0b40b'}
for spec in specs:
 key=spec['key']
 if key=='R1':continue
 text='\n\n'.join(blocks[i]['text'] for i in spec['ids']);steps=[]
 def add(action,key,**kw):
  r=dict(request_id=ident(spec['key']+'/'+key),action=action,**kw);steps.append(r);return r['request_id']
 start=add('start','start',material_id=mid,material_revision=current['revision'],block_ids=[blocks[i]['id'] for i in spec['ids']])
 uid=str(uuid.uuid5(uuid.UUID(start),'root'));root=uid+'/structure';units[key]=uid
 field_ids={};members={}
 def mark(role,value,key,node=root):
  at=text.index(value);step=add('structure',key,unit_id=uid,node_id=node,operation='add',field=role,start=at,end=at+len(value))
  if node==root:
   field_ids.setdefault(role,str(uuid.uuid5(uuid.UUID(step),'field')));members[role]=members.get(role,0)+1
  return str(uuid.uuid5(uuid.UUID(step),'fragment'))
 for role,value in spec['fields'].items():mark(role,value,'field/'+role)
 for i,value in enumerate(spec['conditions']):mark('conditions',value,'condition/'+str(i))
 if spec.get('details'):
  details=spec['details'];a0=text.index(details[0]);end=text.index(details[-1])+len(details[-1]);detail=text[a0:end]
  node=mark('Object',detail,'details')
  add('structure','details/decompose',unit_id=uid,node_id=node,operation='decompose')
  for j,value in enumerate(details):mark('Object',value,'details/'+str(j),node=node)
  add('structure','details/quantity',unit_id=uid,node_id=node,operation='quantity',quantity=[len(details),len(details)])
 for role,count in members.items():
  if count>1:add('structure','quantity/'+role,unit_id=uid,node_id=field_ids[role],operation='quantity',quantity=count)
 add('done','done',unit_id=uid);add('phase','complete',phase='complete')
 result=once(key,'/api/requirements/step',dict(request_id=ident(key+'/save'),action='save-draft',steps=steps));assert result['status']=='saved'
 print(key,'saved',flush=True)
# Link the three complete list-item requirements, then finish the parent again.
parent=call('/api/requirements/session?id='+'88d0787c-f24f-5fbd-83a2-f3a9450b7e13')
old_link=next(n for n in parent['structure_views'][units['R1']]['children'] if n.get('role')=='subrequirement')
steps=[dict(request_id=ident('links/reopen'),action='phase',phase='fields'),dict(request_id=ident('links/remove-old'),action='structure',unit_id=units['R1'],node_id=old_link['id'],operation='remove')]
for key in ['R2','R3','R4']:
 rid=ident('link/'+key);steps.append(dict(request_id=rid,action='structure',unit_id=units['R1'],node_id=units['R1']+'/structure',operation='link',field='subrequirement',target_id=units[key]))
first=str(uuid.uuid5(uuid.UUID(ident('link/R2')),'field'))
steps.extend([dict(request_id=ident('links/qty'),action='structure',unit_id=units['R1'],node_id=first,operation='quantity',quantity=3),dict(request_id=ident('links/done'),action='done',unit_id=units['R1']),dict(request_id=ident('links/complete'),action='phase',phase='complete')])
once('R1-links','/api/requirements/step',dict(request_id=ident('links/save'),action='save-draft',session_id=parent['id'],expected_revision=parent['revision'],steps=steps))
record('units',units)
logic={'R1': {'scope_information': 'Identify the operational journal and its owning aquaculture establishment. A = journals associated with aquaculture establishments. Bind establishment_id, journal_id and the assessment date; these are proposed data concepts, not an agreed Site Model schema.', 'condition_information': 'Within A, determine which establishments are covered by specific record-keeping provisions in Chapters 4, 5 or 6. Proposed logical form: NOT (covered_by_chapter_4 OR covered_by_chapter_5 OR covered_by_chapter_6). B contains journals whose establishments are outside those specific provisions. The excerpt links to these chapters but does not supply their contents. Unknown applicability must remain unresolved, not default to false.', 'verification': 'For each journal in B, require ALL three linked content requirements: R2 AND R3 AND R4. Check that the required information is present and updated in the same assessment context. The phrase at a minimum means at least the listed content; it does not prohibit additional journal information. C contains journals supported by sufficient evidence for all three requirements. Only with adequate coverage and evidence may B ⊆ C support a positive result. Missing evidence is unresolved; a verified missing mandatory item is a failure. This example defines the method and does not produce a verdict.'}, 'R2': {'scope_information': 'Shared scope is inherited through the R1 → R2 Subrequirement link. This entry contains only its own list-item wording; no Subject or modal verb is copied into its decomposition. Identify the operational journal and its owning aquaculture establishment. A = journals associated with aquaculture establishments. Bind establishment_id, journal_id and the assessment date; these are proposed data concepts, not an agreed Site Model schema. R2 inherits the lead-in paragraph from R1 and concerns records of animals and animal products entering and leaving the same establishment.', 'condition_information': 'Use R1’s shared applicability condition through that link. Within A, determine which establishments are covered by specific record-keeping provisions in Chapters 4, 5 or 6. Proposed logical form: NOT (covered_by_chapter_4 OR covered_by_chapter_5 OR covered_by_chapter_6). B contains journals whose establishments are outside those specific provisions. The excerpt links to these chapters but does not supply their contents. Unknown applicability must remain unresolved, not default to false. Preserve both inbound and outbound coverage from brought into and removed from; do not inspect just one direction.', 'verification': 'For each applicable journal, compare its movement records with the relevant known inbound/outbound movements. The required location group is ALL 2: place of origin AND place of destination. Confirm the journal records cover the relevant animals and animal products and that their information is updated. [2,2] counts the two listed information categories; it does not limit the number of movements or records. Evidence sources and record joins are proposed checks requiring an agreed Site Model mapping. Do not infer completeness from a single populated row.'}, 'R3': {'scope_information': 'Shared scope is inherited through the R1 → R3 Subrequirement link. This entry contains only its own list-item wording; no Subject or modal verb is copied into its decomposition. Identify the operational journal and its owning aquaculture establishment. A = journals associated with aquaculture establishments. Bind establishment_id, journal_id and the assessment date; these are proposed data concepts, not an agreed Site Model schema. R3 inherits the lead-in from R1. Identify the establishment’s production units and production method so mortality information can be checked at the stated unit level.', 'condition_information': 'Use R1’s shared applicability condition through that link. Within A, determine which establishments are covered by specific record-keeping provisions in Chapters 4, 5 or 6. Proposed logical form: NOT (covered_by_chapter_4 OR covered_by_chapter_5 OR covered_by_chapter_6). B contains journals whose establishments are outside those specific provisions. The excerpt links to these chapters but does not supply their contents. Unknown applicability must remain unresolved, not default to false. Additionally retain relevant to the production method as a qualification on the mortality-per-production-unit information. Decide the relevant units/representation using the production method. This qualification does not exempt the entire establishment from R1, R2 or R4. The excerpt does not define relevance; keep that decision unresolved when unsupported.', 'verification': 'For every relevant production unit, verify that the journal contains updated mortality information attributable to that unit. The excerpt provides no mortality threshold, mandatory reporting interval or unit of measurement. Do not invent a mortality limit or treat a missing value as zero. The check concerns record coverage and currency, not whether mortality is acceptably low.'}, 'R4': {'scope_information': 'Shared scope is inherited through the R1 → R4 Subrequirement link. This entry contains only its own list-item wording; no Subject or modal verb is copied into its decomposition. Identify the operational journal and its owning aquaculture establishment. A = journals associated with aquaculture establishments. Bind establishment_id, journal_id and the assessment date; these are proposed data concepts, not an agreed Site Model schema. R4 inherits the lead-in from R1 and concerns completed health inspections and their recorded results for the same establishment.', 'condition_information': 'Use R1’s shared applicability condition through that link. Within A, determine which establishments are covered by specific record-keeping provisions in Chapters 4, 5 or 6. Proposed logical form: NOT (covered_by_chapter_4 OR covered_by_chapter_5 OR covered_by_chapter_6). B contains journals whose establishments are outside those specific provisions. The excerpt links to these chapters but does not supply their contents. Unknown applicability must remain unresolved, not default to false. Restrict inspection-result checking to completed inspections. The excerpt does not specify a minimum inspection frequency or require all listed activities to have occurred in every inspection.', 'verification': 'Check the recorded results against the known completed health inspections. The required result group is ALL 5: number of completed inspections AND sampling AND examinations performed AND diagnoses AND treatments carried out. [5,5] counts information categories, not five inspections or five mandatory interventions. Where no sampling, diagnosis or treatment occurred, determine from evidence how that absence is recorded; do not invent an event and do not treat a blank field as proof that none occurred. Verify completeness and currency; inspection frequency and required update deadlines are not stated here.'}}
gaps=['The linked Chapters 4, 5 and 6 have not been supplied here; their applicability cannot be resolved from this excerpt alone.', 'The excerpt requires updated information but supplies no fixed update interval. Agree the assessment date, relevant records and evidence of currency without inventing a deadline.', 'No Site Model records, accepted field mappings or real compliance assessment are included.']
for spec in specs:
 key=spec['key'];ctx=call('/api/interpretations?unit_id='+units[key]);citations=[dict(id=c['id'],quote=c['text'],start=0,end=len(c['text'])) for c in ctx['context']['citations'] if c.get('block_id') in {b['id'] for b in blocks}]
 values={'scope':spec['fields'].get('Subject','Inherited from R1: the operational journal'),'condition':'\n'.join(spec['conditions']) or 'Shared applicability is inherited from R1','demand':' '.join(spec['fields'].get(f,'') for f in ['Modal Verb','Main Verb','Object']).strip(),**logic[key]}
 fields={k:dict(value=v,basis='source' if k=='demand' or (k=='scope' and key=='R1') or (k=='condition' and spec['conditions']) else 'interpretation',references=citations,gaps=gaps if k=='condition_information' else [],state='specified') for k,v in values.items()}
 once(key+'-interpretation','/api/interpretations/save',dict(request_id=ident(key+'/interpretation'),unit_id=units[key],expected_revision=ctx['revision'],context_fingerprint=ctx['context']['fingerprint'],linked_material_ids=[],fields=fields,action='save'))
 print(key,'interpretation saved',flush=True)
for key in ['R2','R3','R4']:
 doc=call('/api/requirements/session?id='+{'R2': 'd121532b-25ab-5f3f-bbb5-8eb60b166028', 'R3': 'f9c26517-2364-5d54-ba94-440f2f26dae2', 'R4': 'da38c439-4fd3-5c81-98db-10e76adff92b'}[key])
 once(key+'-remove-superseded','/api/requirements/step',dict(request_id=ident(key+'/retire'),action='delete',session_id=doc['id'],expected_revision=doc['revision']))
record('pin',call('/api/material-queue/pin',{'material_id':mid,'pinned':True}))
# Portable explanation keeps the provided English JSON intact and states normalization explicitly.
(ROOT/'workbench/examples/example-workbench.json').write_text(json.dumps(dict(schema='workbench-example/1',source_id='ADF-10',source_language='English',provided_schema='example.json',requirements=specs,interpretations=logic,gaps=gaps,normalization=['English demonstration translation based on the user-supplied English JSON; original Norwegian excerpt retained in source history.','Each of the four source passages is a separate Requirement. R2, R3 and R4 contain only their own list items; shared context comes from R1 via explicit Subrequirement links.','Three child Requirements are linked from R1 with All 3. Nested lists of 2 and 5 information categories become source-bound Object groups with exact [2,2] and [5,5]; the provided JSON remains unchanged.','Exact group counts concern the listed mandatory categories; they do not prohibit additional journal content.']),ensure_ascii=False,indent=2))
print('DONE',mid)
