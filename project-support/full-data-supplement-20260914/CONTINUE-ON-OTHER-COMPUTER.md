# Continue on the other computer

Repository: https://github.com/thejaytang/smarter-compliance-aquaculture
Live data assets: https://github.com/thejaytang/smarter-compliance-aquaculture/releases/tag/full-workspace-20260914

## Copyable instruction for the other Codex task

Clone or update the repository source into an isolated test workspace. The main-branch source ZIP alone omits the original runtime data. Read the release's RESTORE.txt, PRIORITY-DATA-INDEX.json, FULL-DATA-INDEX.json, DEFERRED-DATA-INDEX.json and DIRECTORY-INVENTORY-INDEX.json. The 01, 02 and 03 ZIP families contain file data; 04-directories-and-inventory.zip preserves directories and the original metadata inventory. Download and SHA-256 verify every currently uploaded numbered data ZIP, then extract them to the new repository root. Extract 00-current-review-databases.zip LAST. This package has eight simultaneously reserved, SQLite-online-backed-up owning stores; each passed integrity_check. For the eight database paths listed in DATABASE-SNAPSHOT.json, discard matching WAL/SHM files only inside the new restored workspace before opening them. Never overwrite a running workspace.

Verify the restored System1 sources/operations/history contain saved records for Ana Jokic, Daniel Restad and Weijie Tang. Keep their decisions, annotations and personal drafts intact. Build Windows environments following ENVIRONMENT.md and workbench/deployment/setup_windows.cmd; do not try to execute copied macOS .venv binaries. Preserve historical absolute path strings in saved evidence; inspect the existing recovery-mapping/serve-restored implementation for safe relocated data access rather than blindly replacing history text. Do not enable business schedules merely to inspect the restored data.

The Mac task is publishing additional data parts to the same release. Re-read all four indices to pick up newly uploaded assets and verify their checksums. The FULL index counts files deferred during the first pass; subtract paths in DEFERRED-DATA-INDEX.json to identify files already recovered in 03 packages. Once the other computer has started writing its own reviews, download later packages into a staging directory and use the product's existing snapshot/conflict workflow. Do not repeatedly overlay databases onto a running or newly edited test workspace. Do not call the 59.2 GB whole-folder archive complete while the release/index says UPLOADING. Report exactly which data parts were present and whether the required original/Canonical files resolve before declaring the other-machine test ready.

UPLOAD-STATUS.json is the combined live coverage report. It counts unique original inventory paths across main, 01, 02 and 03 packages; full completion requires missing_original_paths = 0 and status COMPLETE. Background upload supervision retries stopped upload workers and reissues outstanding iCloud requests for up to four hours. It stops requesting downloads below 3 GiB free space and reports an incomplete state rather than declaring success.
