# Settings and full snapshot collaboration

User-authorized implementation, 2026-09-14. Supersedes the coordinator-only package/adoption interaction for this scope. Preserve source governance, exact-version human confirmation, originals and historical evidence. Another task owns material interaction edits; use narrow shell integrations.

- One bottom Settings entry, replaced by the selected reviewer's name. Retain reviewer selection.
- Independent top-layer dialogs: Collaboration, Runtime status, Automation. Retain maintenance tools in a secondary dialog.
- Full logical snapshots: originals deduplicated, sources, saved material work, review records and shared synchronization history. Exclude software, caches and backup ZIPs.
- Explicit import preview/apply; version ancestry, idempotence, no wall-clock winner; preserve both sides and require conflict decisions. No administrator-only merge-save step.
- Confidence and inspection schedule settings; save does not run an immediate check. Local schedules are not imported or activated through packages.
- Validate with isolated stores: all-reviewer full round trip, concurrent changes, stale/repeated imports, conflict choices, interrupted apply, tampered archive, and history re-export. UI validation must include modal focus, reviewer switching, export/download, and narrow layout.
- Measure a real snapshot without applying business changes. Record limits honestly; do not claim Windows acceptance.
