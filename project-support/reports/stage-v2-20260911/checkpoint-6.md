# Stage v2 checkpoint 6: actionable dependencies and truthful original ranges

2026-09-11. Decision: **ADJUST the attempted local-delivery shortcut; CONTINUE source-fidelity work with explicit dependencies.** Overall goal incomplete.

## What the dependency probe established

The ten actual Canonical parents remain bound to table, page, shared-context and whole-parsed-range checks. The whole range contains known unresolved natural defects. Treating a generic check as redundant without evidence would weaken the gate; no dependency, threshold or acceptance rule was removed. No A or B business decision was submitted in this checkpoint.

Instead, the detail response now exposes distinct transitive pending content prerequisites using the dependency graph already loaded for the guarded read. The workbench shows their names, original scope and reasons, and opens the exact prerequisite. Accepted ancestors are omitted only when they have no pending prerequisite; missing dependencies and cycles remain explicit. Readiness logic is unchanged. The displayed count is not presented as independent reviewer workload.

## A real wrong-page preview was found and fixed

Opening the actual whole-range prerequisite exposed an unlocated PDF reference being displayed as a bound page-1 crop. The fixture covers physical pages **19, 20, 116, 117, 118, 119, 120 and 128**, not page 1.

Generic PDF coverage views now derive their page inventory from the bound, in-runtime Canonical only after verifying its hash. This includes inventoried pages with no extracted unit. Original stored references and Canonical facts remain unchanged. Each page has a direct source-inspection button; all page dependencies and completeness checks remain in force.

`original-preview/7` no longer substitutes page 1 for missing page locations. An unresolved reference returns an explicit unlocated result without an image. Located multi-page previews show the first actual page and list the other required pages; invalid referenced pages fail explicitly. Expanded full-original URLs use the first bound page as well.

## Actual verification

The [readback](../../../system2/outputs/runs/requirement-acceptance-20260911/stage-v2/dependency-scope-v1/readback.json) records an actual shared workbench instance, the real Canonical row and ten distinct prerequisites. Browser actions opened the whole-range prerequisite, displayed all eight physical pages and navigated directly to original page 128 with mapped regions. The range preview now shows page 19. Expanded full-original iframe and link contain `#page=19`; the embedded PDF was dark in the inspected IAB viewport, so embedded-reader rendering is not claimed as visually accepted. The source-bound PNG and explicit page navigator were separately inspected and work.

Twenty detail reads (ten real-row and ten whole-range reads) have p95 **0.230 seconds** on this one-document, 109-item fixture. This is an additional bounded read benchmark, not a replacement for the earlier full-load measurements. Saved event and Excel event both remain **13**, with zero published Requirements. The stored original references are unchanged.

Final checks: **848 System2 tests passed, one existing fixture skip**, plus **25 frontend tests passed**. Tests cover transitive deduplication, accepted/missing/cyclic ancestors, escaping/navigation, frozen Canonical page inventories including no-output pages, changed-Canonical fallback, unlocated PDF behavior and complete index validation. Regression success does not establish source quality.

## Version and remaining gates

`requirement-stage-v2-candidate-03` freezes 378 program/test/dependency files. [Manifest](../../../system2/outputs/runs/requirement-acceptance-20260911/stage-v2/candidate-freeze-v3/manifest.json): archive SHA-256 `32e341099efcbe51d8a7c109b66e519881559e6a770a3b6cd55b32f3581d65d3`. All archive entries and current source hashes match, candidate 01/02 archives are unchanged, and all 91 frozen baseline files retain their hashes. Excel remains renderer 21. The last broad normal business/original/Gold audit is the unchanged CP5 audit; this checkpoint performs read-only navigation and preview-cache generation only.

Parser `candidate-rerun-03`, verifier `original-page-comparison/6`, classifier `literal-requirements/3` and all raw quality/automatic-release numbers are unchanged. Q18 actual accepted-A delivery remains UNMEASURED. Protected normal startup, native Office visual checks and final Canva draft commit remain pending the previously requested user input. No reset was consumed; latest Codex window usage is 41% used.

## Next discriminating work

Return to the four undetected natural cross-page relationships. Extend the retained original-side geometry probe with original caption/header scope and explicit negative controls before considering production verifier integration. Compare against the same frozen errors and controls; do not treat a generic continuity question as a confirmed missing relationship. Retain the observed ambiguity and stop the inference path if the added evidence cannot separate continuation from a new table.
