# ADF-10 example replacement and Requirement header follow-up

Verified locally on 2026-09-16 at http://127.0.0.1:62742/.

## Installed example

The user supplied `example 1.html`, `example 1.md` and `example 1 2.json`. The displayed material is still **example (PE001)**, pinned first for Weijie Tang. Its current source snapshot is **PE001-002**, material `ad7738ca612922619d85d725080cc26e`. The prior example/source remains recoverable; pending material review now shows one current entry when a replacement is present. An absent successor does not hide the only available material, and archive records remain visible.

The original HTML and Markdown wrappers were removed without changing source words or chapter links. The supplied English JSON is preserved byte for byte as `workbench/examples/example.json`. The [portable normalization](../../workbench/examples/example-workbench.json) documents four complete entries: the shared lead-in R1 and three children, each bound to the original lead-in and its own bullet. R1 links all three child roots. The location and health-result groups retain `[2,2]` and `[5,5]`.

All four have explicitly saved six-field interpretations, proposed English evidence logic, unresolved chapter applicability and currency requirements. No update deadline, mortality threshold, mandatory medical event or compliance verdict was invented. Interpretation revision 1 for each entry is current, unreviewed and linked through the saved decomposition/material versions to source PE001-002. API trace readback verifies one original source segment for R1 and two for each child. The example remains personal saved demonstration content, not adopted authority.

## Interaction

The entire Requirement top row toggles expansion, excluding Locate / Auto-extract / Remove. Coloured source wording is outside the toggle region and remains visible when collapsed. Only one entry opens. The Rx button retains keyboard operation and aria-expanded. Auto-extract is disabled and explicitly Not connected; the third-pane global Re-extract was removed. The earlier-inline history notice is removed from everyday editing without deleting saved history.

Normal-browser checks exercised a click in the top-row gap outside the arrow, collapse/reopen, a source-text click without toggling, Remove followed by Cancel, and Locate without collapsing. R1's three external links and its populated fourth-pane Logic were read back. The active UI has no earlier-inline notice and no global third-pane Re-extract.

## Validation and boundaries

- 340 frontend tests passed.
- 289 Workbench backend tests passed; all 18 material-queue tests passed again after handling source observation that predates successor opening.
- Controlled activations snapshot all 11 owning stores, with unchanged existing business rows across restart, clean foreign-key checks and matching served UI assets. Business mutations for the replacement used the existing owner APIs and retain earlier history.
- The model is Not connected; no live API, query or legal-currentness verification was performed. Windows-native desktop interaction remains unverified.
- Portable example assets and source code can be published. Runtime receipts, personal databases, local backup copies and input staging files remain local.
