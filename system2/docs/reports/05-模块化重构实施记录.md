# 模块化重构实施记录

## 2026-09-01 Checkpoint 1

决策：`CONTINUE`。

### 已实施

- 新增跨模块 `contracts`，区分 Canonical、evidence 和 verification contracts。
- 新增 `preflight`、`evidence`、`extraction`、`canonical`、`verification`、
  `domains`、`delivery`、`orchestration` 公共边界。
- 将 1,605 行根 pipeline 移到 `orchestration/pipeline.py`，并提取 runtime helpers、
  region helpers 和 critical-conflict review routing；剩余 pipeline 为 1,453 行。
- 旧 pipeline import path 保留 lazy facade。
- 新增 verification depth、atomic assessment 和 review disposition contract；人工审查
  对所有机器 depth 生效。
- 调整 ontology、schema validator、package facade 为 lazy import，降低轻量模块对
  重依赖的耦合。
- 更新 `README.md`、`AGENTS.md`、`PROJECT_STATE.md`、architecture index 和迁移计划。

### 已验证

- `compileall`：通过。
- config/runtime smoke：通过；验证 verification policy 可加载，parser-owned cleanup
  删除已知产物并保留未知用户文件。
- verification contract tests：3 个直接 assertions 通过。
- module dependency boundary tests：2 个直接 assertions 通过。
- unresolved critical-conflict routing smoke：通过。
- source-tree fingerprint：
  `5d24d42a63f34089e8d9673db2489988bff2ff8064373cfe127f2971addf525b`。

### 未完成验证

完整 `pytest` 与完整 pipeline import 未完成。项目 `.venv` 中至少以下依赖处于
macOS `compressed,dataless` 状态：

- `_pytest/config/__init__.py`
- `numpy/__init__.py`
- `jsonschema/exceptions.py`

因此不能声称完整回归通过，也不能把本 checkpoint 解释为 parser behavior 或
真实 PDF fidelity 已验收。

### 下一实验

恢复 `.venv` 本地物化后，依次运行：contract tests、原有定向 tests、完整 tests、
固定小样本 artifact hash comparison。只有这些通过后才继续拆分 pipeline stage
runners。

## 2026-09-01 Checkpoint 2

决策：`CONTINUE`。

### 已实施

- 依据 `uv.lock` 重建根目录 `.venv`，启用 `dev` 与 `table-fallbacks`；旧 dataless
  环境 `.venv.dataless-backup-20260901` 后续已在用户确认后删除。
- 新增 `RunContext`、`RunPaths`、`FinalizationInput` 和 `FinalizationStage`，把
  workspace preparation、resume/fingerprint、Canonical validation 和 delivery
  finalization 从大 pipeline 中拆出。
- `orchestration/pipeline.py` 从 1,453 行降至 1,299 行；根 `pipeline.py` lazy
  compatibility facade 保持不变。
- EvidenceSpan schema 升至 1.5，增加 `review_threshold`、`review_reason_codes` 和
  `evidence_path_ids`。
- 逐 evidence span 的 confidence policy 已接入生产 reconciliation。低 confidence、
  disagreement、provenance 缺失和机器 depth 路径不足均 fail closed 到 targeted
  human review；机器已 resolved 不再等于免人工审查。
- 新增 `verification-report.json`，并纳入 parser-owned artifact cleanup。
- human review service 只有在同一 owner 下所有 span 均不再 ambiguous 且不再
  `requires_human_review` 时，才解除 block review 状态。
- `pytesseract` 改为真正的调用时导入，避免轻量 pipeline import 隐式加载
  pandas/pyarrow。

### 验证证据

- contract、model、routing、API/worker 定向回归：`34 passed`。
- 机器 depth 与 atomic routing 追加回归：`10 passed`。
- 第一次全量：`337 passed, 1 skipped, 1 failed`；唯一失败为环境未安装已声明的
  `table-fallbacks/img2table`，不是代码断言差异。
- 安装该 extra 后，失败项单独重跑：`1 passed`。
- 最终全量回归：`340 passed, 1 skipped`；唯一 warning 是 Starlette 当前仍使用
  `httpx`、未来建议迁移 `httpx2` 的 deprecation warning。
- pytest 生成的一页 PDF 正式通过完整 pipeline：Canonical schema `1.5`，1 页、
  4 blocks、3 atomic assessments、`source` depth、automatic coverage `1.0`、0 review、
  0 validation errors。
- 最终全量运行生成的该样本 `canonical.json` SHA-256：
  `e9f6c87a113e2807fc864b00bef4a86bf2ae946770fe4717af7307862003e74a`。
- 同次运行的 `verification-report.json` SHA-256：
  `824b05982554d3f691c479f2a90667bbc651f7b3ea0d175ea6ebe7a45e7afe4f`。
- 当前源码 fingerprint：
  `7b0af6ce9ad83a16a89121dd399b6cb15af9b9978c765ed491aead92e24f27d2`。

### 边界

- 本轮没有运行整份大型 PDF，没有修改输入 PDF、Gold annotation/manifest 或历史
  baseline。
- 当前 `source` depth 已形成生产闭环；`independent`/`strong` 在没有独立证据路径
  时会进入 review。独立 verifier 和 confidence calibration 仍属于后续 checkpoint。
- 页面 extraction/assembly 主体尚未全部移出 pipeline。下一阶段必须先为固定真实
  PDF 建立 artifact comparison，再继续拆分，避免只追求文件变短造成静默行为漂移。
