# Review workbench v2 implementation

The user approved full-clause/table review, evidence-bound previews, indexed per-unit persistence, and a full-text source-per-sheet Excel view on 2026-09-09.

## Current execution

1. Indexed persistence and backward-compatible document readers; guarded unit decisions and scoped invalidation.
2. Human task descriptions, source-position groups, isolated original previews, task-specific controls and explicit System1 submission.
3. Migrate a production copy, test real sources and 30k+ indexed reads, then quiesce writers, back up and migrate production.
4. Validate browser actions in isolation, Excel structure/native presentation, and record measured acceptance and remaining limitations.

Preserve original files, Canonical, all historical decisions and pre-migration database backups. Grouping is a versioned derived projection, never source acceptance. Existing named decisions must not be expanded to unseen content. No API activation or System3 semantic execution is authorized by this implementation.

## Checkpoint, 2026-09-09

Implemented indexed storage and production migration, two primary pending levels, focused review actions, safe original-region evidence, background full-text Excel, System1 explicit submission/confidence view and source-issue handoff. Production-copy preservation and real HTML browser gates passed. Native Excel and loopback timings are recorded in [acceptance](../reports/20-review-workbench-v2.md). Continue source review and independently labeled calibration; do not reinterpret engineering checks as complete source accuracy, Norwegian PDF acceptance, live API effectiveness or System3 consumer acceptance.
